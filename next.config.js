/**
 * @type {import('next').NextConfig}
 */

// basePath: '/dho-cal',
// assetPrefix: '/dho-cal/',
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
